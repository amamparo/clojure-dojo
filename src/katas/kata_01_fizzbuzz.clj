(ns katas.kata-01-fizzbuzz)

;; ─── Kata 1: FizzBuzz ──────────────────────────────────────────────────
;;
;; Implement `fizzbuzz`. Given a non-negative integer `n`, return a
;; vector of length `n`. The element at 1-based position `i` is:
;;
;;   "FizzBuzz"  if i is divisible by 15
;;   "Fizz"      if i is divisible by 3
;;   "Buzz"      if i is divisible by 5
;;   otherwise   the number i itself (an integer)
;;
;; Examples:
;;   (fizzbuzz 0)  => []
;;   (fizzbuzz 5)  => [1 2 "Fizz" 4 "Buzz"]
;;   (fizzbuzz 15) => [1 2 "Fizz" 4 "Buzz" "Fizz" 7 8 "Fizz" "Buzz"
;;                     11 "Fizz" 13 14 "FizzBuzz"]

(defn fizzbuzz [n]
  ;; TODO
)
