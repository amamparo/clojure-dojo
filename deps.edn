{:paths ["src" "resources"]
 :deps  {org.clojure/clojure {:mvn/version "1.12.0"}}
 :aliases
 {;; Test path only. No :main-opts, so this alias is safe to enable
  ;; for an editor REPL jack-in (e.g. Cursive).
  :test {:extra-paths ["test"]}}}
